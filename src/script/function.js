// 向量
function vector (a, b) {
    // body...
    return {
        x: b.x - a.x,
        y: b.y - a.y
    }
}
// 向量叉乘
function vectorProduct (arguments) {
    // body...
    return v1.x * v2.y - v2.x * v1.y
}